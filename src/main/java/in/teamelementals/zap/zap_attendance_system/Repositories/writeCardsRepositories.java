package in.teamelementals.zap.zap_attendance_system.Repositories;

import in.teamelementals.zap.zap_attendance_system.model.writeCards;
import org.springframework.data.jpa.repository.JpaRepository;

public interface writeCardsRepositories extends JpaRepository<writeCards,Integer> {
}
